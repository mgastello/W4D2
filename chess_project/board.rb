class Board
    def initialize
        @grid = Array.new(8) { Array.new(8) }   # insert null piece
        # (2..5).to_a.each do |idx1|
        #     @grid[idx1].each_with_index do |ele, idx2|
        #         @grid[idx1][idx2] = # instance of null piece
        #     end
        # end
    end

    def move_piece(start_pos, end_pos)
        
    end
end

